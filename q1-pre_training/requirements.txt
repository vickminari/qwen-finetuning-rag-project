# Dependências para a Q1 - CPT (Continued Pre-training)
# Instale a versão do PyTorch compatível com sua CUDA antes de instalar as demais.
# Exemplo para PyTorch com CUDA 12.1 ou 11.8:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# Dependências principais:
transformers>=4.45.0
peft>=0.12.0
trl>=0.10.0
datasets>=3.0.0
accelerate>=0.34.0
bitsandbytes>=0.43.0
tqdm>=4.66.0
packaging

# NOTA: O Unsloth para Windows deve ser instalado seguindo as instruções oficiais.
# Geralmente, utiliza-se:
# pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
# Ou a instalação via pip direta caso já esteja no canal adequado.
